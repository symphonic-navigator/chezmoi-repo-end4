# You can put custom rules here
# Window/layer rules: https://wiki.hyprland.org/Configuring/Window-Rules/
# Workspace rules: https://wiki.hyprland.org/Configuring/Workspace-Rules/

# ######## Window rules ########

# Uncomment to apply global transparency to all windows:
#windowrule = opacity 0.89 override 0.89 override, match:class .*

# Disable blur for all xwayland apps
# windowrule = no_blur on, match:xwayland 1

# Special workspaces
workspace = special:kitty-quake, on-created-empty: [fullscreen] bash -c "kitty --class 'kitty-quake'"
workspace = special:music, on-created-empty: flatpak run com.spotify.Client
#workspace = special:tk, on-created-empty: bash -c "$HOME/.local/share/multibrowser/start_multibrowser.sh --config $HOME/.multibrowser/tabs.json"
workspace = special:comms, on-created-empty: discord

